# LangChain pulls in an unused optional peer (typeorm/better-sqlite3) that trips
# npm's strict peer resolution. This makes `npm install` work without the
# --legacy-peer-deps flag, which matters on hosts (Render/Railway) that run a
# plain `npm install`.
legacy-peer-deps=true
